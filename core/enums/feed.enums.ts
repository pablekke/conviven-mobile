// Enums de Feed (específicos del feed)

export enum MatchActionType {
  LIKE = "like",
  PASS = "pass",
  SUPERLIKE = "superlike",
}

export enum CleaningFrequency {
  DAILY = "daily",
  WEEKLY = "weekly",
  MONTHLY = "monthly",
}

export enum RoomieVerificationStatus {
  UNVERIFIED = "unverified",
  PENDING = "pending",
  VERIFIED = "verified",
}
